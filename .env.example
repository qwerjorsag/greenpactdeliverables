PORT=3000
DATA_AUTH_SECRET="change-this-secret-before-production"
DATA_TOKEN_TTL_SECONDS=28800
DATA_ADMIN_USERNAME="komora"
DATA_ADMIN_PASSWORD="hospodarskakomora2026"

# Allowed Source Tokens (Comma separated)
ALLOWED_SOURCE_TOKENS="H7K2-ABC,EU-2026-XYZ,TEST-TOKEN"

# Secret for simple rate limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10

# Optional: set only if the frontend is served from a different origin than the API.
# Leave empty when frontend and backend run on the same origin.
# In Docker, the app also reads this value at runtime from /config.js.
VITE_API_BASE_URL=
